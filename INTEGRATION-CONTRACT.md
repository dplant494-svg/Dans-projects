# Integration contract — TSC Rig Reporting Tool ↔ Rig Visit Dashboard

**Audience: anyone (human or Claude) editing the TSC Rig Reporting Tool
(`WCE_Rig_Vist_Reporting_Tool_V*.html`).**

A separate dashboard pipeline consumes the `.json` files this tool exports.
It lives in the `dplant494-svg/Dans-projects` GitHub repo: a PowerShell
scanner (`scripts/Update-Dashboard.ps1`) reads every export from a shared
folder, and `dashboard/dashboard.html` (served from IIS) displays the visit
summaries and renders the full reports. **If you change what the tool
exports, you can silently break that dashboard.** This file is the contract:
what must not change, and what is safe.

> **Scope note (v2.0):** this contract now covers BOTH report tools — the TSC
> Rig Reporting Tool (WCGRRT) and the Seadrill Subsea Onboard Reporting Tool
> (SSORT). The scanner accepts **any `.json` file with a valid payload
> regardless of filename** and scans subfolders, so filename drift no longer
> breaks discovery — but the naming convention below is still the standard.
> SSORT's `meta.reporttype` is ingested and drives the dashboard's type
> filter; when absent, the type is derived from which tile data block is
> present (`cbmData` → CBM Inspection, `sbopData` → Surface BOP Testing,
> `pdcData` → Pre-Deployment Checklist, `caData` → Conditional Assessment,
> `inspData` → Technical Inspection; none → Rig Visit).

## Never change these (hard dependencies)

1. **Export filename convention** (naming is no longer load-bearing for
   discovery, but keep it for consistency and collision-avoidance)
   WCGRRT: `seadrill-report_<rig>_<YYYY-MM-DD>.json`
   SSORT: `seadrill-report_<rig>_<YYYY-MM-DD>_<reporttype>.json`
   The `.json` extension IS required.

2. **Top-level payload shape** (currently `version: 3`)
   ```
   { version, exportedAt, meta, tiles: [], criticalRows: [], actionRows: [], ... }
   ```
   `meta` must remain a top-level object — the scanner skips any file
   without it. `tiles`, `criticalRows`, `actionRows` must remain arrays.

3. **Summary fields the scanner extracts** (dashboard table/KPIs/charts):
   | Field | Type / format | Used as |
   |---|---|---|
   | `meta.asset` | string | rig name (grouping key) |
   | `meta.date` | `"YYYY-MM-DD"` string | visit start — **keep this exact format** |
   | `meta.dateend` | `"YYYY-MM-DD"` or `""` | visit end |
   | `meta.wce` | string | WCE Technical Superintendent |
   | `meta.location` | string | well / location |
   | `meta.type` | string | visit classification |
   | `meta.discipline` | string | discipline |
   | `tiles.length` | — | "Daily reports" count |
   | `criticalRows[]`: `done` **(boolean)**, `equip`, `sfi`, `date`, `issue`, `mit` | | critical items + open/closed counts |
   | `actionRows[]`: `desc`, `sys`, `resp`, `target`, `deadline`, `leftWithRig` **(boolean)** | | action items |

   `done` and `leftWithRig` must stay real booleans — strings like `"true"`
   would corrupt the open/closed counts.

4. **Fields the dashboard's full-report viewer renders** (keep names/types):
   - `meta`: `sss`, `rigmgr`, `oim`, `arigmgr`, `tsl`, `elec`, `dsl`, `tech`,
     `mpd`, `wo`, `other[] = [{ name, title }]`, `summary24`/`next24`
     (HTML strings)
   - `tiles[]`: `title`, `status`, `fodb`, `tileDate`, `notes` (HTML),
     `imgData` (`data:image/...` URI or `""`),
     `equipEntries[]`: `type`, `manual` (bool), `manualName`, `manualSfi`,
     `ramSize`, `surfaceTest`, `flagEot`/`flagCrit` (bools), `notes` (HTML),
     `photos[]` (data URIs), `captions[]` (parallel to photos)
   - `tiles[].vsrData` (or `null`): scalar fields (`vendor`, `surveyDate`,
     `surveyType`, `contactName`, `contactEmail`, `preparedBy`, `location`,
     `oem`, `eqDesc`, `partNum`, `serialNum`, `vsrSfi`, `reportDate`,
     `acceptComments`, `concerns`, `actions`, `forecast`) and
     `sections[] = [{ title, items: [{ label, checked, sub }] }]`
   - Tile `status` strings are classified by keywords: *pass/operational/ok*
     → blue pass, *monitor/watch/partial* → orange, *fail/critical/down* →
     red. Keep those words in any new status wording.

5. **Report-saving plumbing** — do not remove or rename:
   `buildReportPayload()`, `exportToFile()`, `postReport()`,
   `sdWriteToReportFolder()`, `chooseReportFolder()`, `sdPostReport()`, and
   the `REPORT_POST_URL` constant (currently `''`; a server receiver is
   planned — when it exists this gets set to a same-origin URL, so keep the
   POST format: JSON body, `Content-Type: application/json`, filename in the
   `X-Filename` header).

## Now load-bearing: keys the dashboard actively consumes (do not rename)

These started as "unknown keys, safely ignored" but the dashboard pipeline now
reads them. Renaming, retyping, or restructuring any of these breaks a live
dashboard feature — treat them exactly like the core fields above:

| Key | Consumed by |
|---|---|
| `meta.reporttype` | report-type filter/labels on the reports dashboard |
| `meta.discipline === "Planning"` or `meta.planningOnly` **boolean** (WCGRRT REV 115+) | Excludes the report from the rig-visit Reports Dashboard list entirely — Planning/BWM Weekly reports belong only on the BOP Fleet Planning Dashboard. Checked at the scanner level (`Update-Dashboard.ps1`, before the record is added to `reports-data.js`), not client-side — the report never reaches that data file. Its `tiles[].planningData`/`bwmData` are still captured separately into `planningReports`/`bwmSnapshots` as usual, unaffected by this exclusion |
| `meta.logMonth` + `meta.dayLog[]` / `meta.dayLogMonth[]` (`date`, `shift`, `personnel`, `equip`, `failure`/`lesson` **booleans**, `note` HTML, `photos[]`) — SSORT REV 95 renamed the monthly array from `dayLog` to `dayLogMonth`; both are read. **Confirmed in a real export**: a report can carry both keys at once — `dayLogMonth: []` (an empty stub) alongside the real entry still in the legacy `dayLog[]` — so any client code choosing between them must check `.length`, not truthiness (`dashboard.html`'s full-report viewer had exactly this bug: `meta.dayLogMonth || meta.dayLog` always picked the empty array, since `[]` is truthy in JS, silently hiding the real Daily Log/Lesson Learned content in the full-report viewer even though the scanner's own `dailyLog` aggregate — built in PowerShell, where `if (@())` is falsy — was unaffected and always had it right; fixed in v2.27-era `dashboard.html`) | Daily Logs & Lessons Learned index. **Dedup key is rig+date+shift+EQUIP (v2.35+, per the SSORT REV 139 distribution handoff `DASHBOARDMONTHLYLOGDISTRIBUTIONHANDOFF.md`)** — date+shift alone is not unique, a rig can log several entries per day for different equipment. The same day arrives by up to FOUR routes (per-entry auto-post, lesson-learned post, single-day re-post, monthly roll-up — the roll-up shares the `seadrill-report_` prefix with rig visits, distinguished only by `meta.reporttype`, which is why routing is never filename-based). A file carrying `meta.dayLogEntry` (routes 1–3) has ONLY that entry ingested — its ride-along `dayLog`/`dayLogMonth` month arrays are ignored, or every entry post would re-ingest the whole month. An individual-entry record OUTRANKS a monthly-roll-up record for the same key regardless of file times (the roll-up is a manual snapshot); within the same source rank, newest file wins (re-posts are normal). The full-report viewer applies the same rule: opening a route-1 file shows only the posted entry, opening the monthly roll-up shows the month |
| `meta.logDate` + `meta.dayLogEntry` (single object, same shape as one `dayLog[]`/`dayLogMonth[]` item) + `meta.hasLessonLearned` (bool) | SSORT's immediate per-day posts (`reporttype` `"Daily Log Entry"` / `"Lesson Learned"`) — same dedup index as above; `hasLessonLearned` ORs into the entry's own `lesson` flag |
| `tiles[].bwmData` (`week`, `reportDate`, `compiledBy`, `rows[]` incl. `m.*` booleans) | BOP Fleet Planning Dashboard (weekly snapshots + history) |
| `window.BWM_DATA.excelSnapshots[]` (new, `Update-Dashboard.ps1` v2.27+) — sibling array to `snapshots`, one entry per weekly BWM planning workbook found in the report folder: `{ file, mtime, base64 }`. Planners now drop their `*_BWM_Reporting*.xlsx` workbook straight into the shared folder instead of re-entering it into WCGRRT; the scanner never parses the workbook (no Excel dependency in PowerShell) — it just base64-carries the raw file. `bop-dashboard/dashboard.html`'s `parsePipelineExcelSnapshots()` decodes it, reads the `BWM Status Report` sheet with the vendored SheetJS (`xlsx.full.min.js`) via the same `parsePlannerSheet()` already used by the manual "Weekly Planner" upload box, and folds the result into the same `{week, reportDate, compiledBy, rows}` snapshot shape `bwmData` produces — so Excel-sourced and tool-exported weeks interleave in one history/dropdown. `week` is parsed from the filename (`YYYY...Week NN`); `reportDate`/`compiledBy` come from the row with the latest `lastUpdated` value. Daily reporting (`reports-data.js`) is a completely separate scan pass, untouched by this | BOP Fleet Planning Dashboard (weekly snapshots + history) |
| `tiles[].planningData` (`reportDate`, `dataDate`, `reportingDay`, `pctComplete`, `planVariance`, `criticalPath`, `simops`, `comments`, `milestones[]`, `done[]`, `next[]`, `projectComplete` **boolean**) | Report-type detection ('Planning Report'); rendered in the reports-dashboard viewer and, per rig, in the BOP dashboard's click-through panel. **Note:** in real exports observed so far this tile is present but every field is empty except `reportDate` — real per-rig reports with actual content are what populate the dashboard views; the empty stub is deliberately ignored (see `Test-PlanningHasContent` in the scanner). `projectComplete: true` on a rig's newest report closes out that rig's BOP-dashboard panel (badged "Project Complete", final numbers stay visible) until a new project's first report supersedes it — `Test-PlanningHasContent` treats `projectComplete: true` alone as real content so a completion-only final submission still gets captured. **Completion is NEVER latched (v2.32+, WCGRRT REV 147 handoff)**: the tool sends an explicit `projectComplete` boolean on every planning export (unticked = `false`, never omitted), and the scanner tracks the newest explicit value per **`rig｜bopNo`** OUTSIDE the content gate, overriding the displayed panel's flag — so an "untick" submission with nothing else filled in clears a previously complete project (previously it was invisible to the content gate and the old `true` latched: the real West Gemini case). A different BOP's reports never touch this panel's flag; reports predating the boolean (key absent) say nothing about completion either way. Completion is read from the flag ONLY — never inferred from % complete, milestone actuals, or dates. Also per REV 147: `meta.float` is now planner-typed **free text** (render as given, never parse/sort/chart it), and `planningData.comments` is consistently `<ul><li>…</li></ul>` (render as HTML; anything stripping tags must not run items together — tag→space is fine) |
| `tiles[].r53Data.fields` / `tiles[].caData.fields` with `s53_*` keys (esp. `s53_isfailure`, `s53_component`, `s53_item`, `s53_compmfr`, `s53_model`, `s53_obsfailure`, `s53_malfunction`, `s53_rootcause`, `s53_findings`, `s53_lessons`, `reportDate`) | R53 events in the lessons/failures index + S53 viewer |
| `tiles[].cbmData` (`equip`, `date`, `rcpt_*`, `<prefix>_g<n>_summary`) — graded checklist items come in **two** key shapes, both read: old `<prefix>_g<section>_<item>_[gr\|cm\|ph]` (2 numbers, "g" prefix) and newer `<prefix>_<major>_<minor>_<item>_[gr\|cm\|ph]` (3 plain numbers matching SSORT's own on-screen reference, e.g. "9.1.5"). Items are discovered from **any** of `_gr`/`_cm`/`_ph` present, not `_gr` alone — most new-format rows are comment-only with no grade key at all. Grade values are strings `'1'`–`'4'`, the literal `'N/A'`, or absent — never booleans/numbers, and `'N/A'` is a distinct state from "no grade key at all," not the same thing. Summary keys (`_g<n>_summary`) are unaffected by the new numbering and unchanged. **`equip` is always the equipment CLASS** (a fixed inspection template, e.g. `"Gate Valves"`), confirmed against real exports where several physically distinct instances (Choke Line Isolation Valve, Kill Line Isolation Valve, Gas Bleed Dual Valve) all carry `equip: "Gate Valves"` — the specific physical instance only appears in `rcpt_model`/`rcpt_serial`. A few equipment classes (confirmed: C&K Stabs) have **no `_gr` key at all** for some items — grade is instead a `"Grade N - ..."` prefix inside the `_cm` comment text, alongside separate `_st`/`_ct` keys whose meaning isn't yet confirmed (both ignored by current parsing). The `cbm_<equip>_` key prefix is built by replacing **every** non-alphanumeric character in `equip` with its own underscore, not just spaces — confirmed `"Ram Block::Shear"` → `cbm_Ram_Block__Shear_` (two colons, two separate underscores, not collapsed to one) and `"C&K Stabs"` → `cbm_C_K_Stabs_`. `rcpt_model` free text is occasionally wrong in source data (one real Upper SBOP export's `rcpt_model` field contained a different component's part number, apparently copy-pasted) — this is a source-data quality issue for Dan/SSORT to know about, not something the dashboard silently corrects | CBM rendering in the full-report viewer; `cbmGrades` aggregate (see below) |
| `window.DASHBOARD_DATA.cbmGrades[]` (new, `Update-Dashboard.ps1` v2.26+) — top-level array sibling to `reports`/`dailyLog`, one row per graded/commented CBM checklist item per report: `{ rig, class, equip, itemKey, itemLabel, itemShape, sortKey, grade, comment, photos, date, file }`. `class` = the equipment template (from `cbmData.equip`); `equip` here is the per-instance label derived from `rcpt_model` (manufacturer prefix stripped), falling back to `rcpt_serial`, falling back to `class` — **do not confuse this `equip` (instance label) with `cbmData.equip` (class) upstream**, they answer different questions. Newest-first, not deduped away across dates (unlike `dailyLog`, which dedupes per-day) — every inspection date for the same item is kept, since that's what the CBM Heatmap's history drill-down reads. No photo binary data, count only | CBM Heatmap tab on the Reports Dashboard |
| `meta.checks` (`reporttype: "Daily Checks"` or `"FLM"`) — `{ rig, kind, prefix, date, shift, by, supervisor, values: {...}, alarmPhotos[] }`. **Lives at `meta.checks`, not inside `tiles[]`** — unlike every other payload type, so it needed its own top-level check in both the scanner and the full-report viewer rather than a `tiles[]`-loop addition; `meta.reporttype` already routed these correctly with zero code change (report-type filter, "not a recognized export" skip logic). `values` is a flat dictionary, one key per reading: `<prefix>_<system>__<item>` (system/item split on the **first** `__`; system/item names may themselves contain single underscores, e.g. `hp_compressor_1__cooling_water_temp` → system `hp_compressor_1`, item `cooling_water_temp`). Five companion suffixes ride on a base key rather than being their own reading — `..._unit` (confirmed real: `..._cooling_water_temp_unit: "°C"`), `..._cmt` (a comment), and since scanner v2.42 (SSORT REV 145 `checkItemDisp`, production-timeline reply 10.4) `..._psi` (the pressure alongside a `tons` load), `..._tp` / `..._dp` (test / differential pressure on an `rb` readback) — merge onto the base only when that base key genuinely exists as its own reading; an orphaned companion key is kept standalone rather than dropped (legitimate: a pressure with no load). The base and its `_psi`/`_tp`/`_dp` are one reading to a human and render together (`180 tons / 2150 psi`, `ok / TP 5000 / DP 250`), on the readings matrix and in the full-report viewer; v2.41 and earlier listed each as a phantom standalone reading. Pass/fail items store the literal string `"pass"`/`"fail"`; anything else (numeric readings, comma lists like `"1,2"`, ratios like `"0.4 / 0.6"`) is a free-text value, passed through verbatim. **Confirmed on a real export**: at least one item (`ccc_faults_alarms`) uses `"pass"` itself as the attention-worthy value, with the `_cmt` comment as the real "something's wrong" signal — comment presence, not the pass/fail value alone, is what should drive any "needs attention" styling, both here and anywhere else this data is read | Full-report viewer's Daily Checks/FLM section; `rigChecks[]` aggregate (see below) |
| `window.DASHBOARD_DATA.rigChecks[]` (new, `Update-Dashboard.ps1` v2.29+) — top-level array sibling to `reports`/`dailyLog`/`cbmGrades`, one row per reading per submission: `{ rig, kind, date, shift, by, supervisor, system, item, itemKey, value, unit, pass, comment, photos, file }`. `kind` is `"Daily Checks"` or `"FLM"`. `pass` is `true`/`false`/`null` (`null` for readings that aren't pass/fail items at all). Deduped by `rig｜date｜shift｜itemKey` (Daily Checks) — FLM has no shift, so its key naturally collapses to `rig｜date｜itemKey` — newest file wins per key. Newest-first, not deduped across dates (same as `cbmGrades`) — every submission date for the same item is kept, since that's what the Rig Monitoring tab's history drill-down reads. `photos` is the submission's `alarmPhotos` count, not per-reading (every reading row from the same submission carries the same count) | Rig Monitoring tab on the Reports Dashboard (fleet tile grid + per-rig readings matrix + per-item trend charts), and the report list's red ⚠ attention flag — a report row is flagged when any `rigChecks` row sharing its `file` is attention-worthy (pass:false OR non-empty comment) |
| **Cross-dashboard read (Reports Dashboard → planning data)**: the Rig Monitoring tiles mirror the BOP Fleet Planning Dashboard's per-rig status — a **single-stack** rig whose BOP status matches `Performing Maint.` shows a "BOP on deck — under maint. / daily checks suspended" badge, and the fleet tile list includes every rig in the latest planning snapshot even if it has never submitted checks. To do that, `dashboard/dashboard.html` dynamically loads `bop-planning-data.js` (and `xlsx.full.min.js` when `excelSnapshots` are present — **in production all current weeks come via the Excel path**, so that's the one that matters) with a path fallback: `../` first (deployed layout: `sacred\dashboard\` page, `sacred\` data) then `../bop-dashboard/` (repo layout). It ports a minimal subset of the BOP dashboard's snapshot parsing (`bwmParseSheet`/`bwmLatestSnapshot`, reading only Rig / BOP / BOP Status / Last Updated columns) and picks the latest week by the same reportDate-then-week sort. **Graceful degradation is contractual**: if neither data file nor SheetJS loads, tiles render without badges and nothing else breaks — never let a planning-data problem take down the reports view | Rig Monitoring tile grid on the Reports Dashboard |
| `tiles[].marineData` (`meta.discipline: "Marine"`, `meta.reporttype: "Marine Integrity Report"`, WCGRRT REV 145+ — see `DASHBOARDMARINEINTEGRITYHANDOFF.md`) — one flat object per report. Pre-computed metrics: `avg_pol`/`avg_reg`/`avg_eqp`/`avg_overall` (number or **null** = nothing scored in that section — render as "not scored", never 0), `target` (currently 3.0), `scoredCount`. RAG rule mirrors the tool: below target red, at/above green. Scored items: `mi_(pol\|reg\|eqp)__<item_slug>` = `""`\|`"1"`..`"4"` (**4 good, 1 very poor** — opposite polarity to CBM grades where 1 is best; `""` = not scored, excluded from averages) with `_cmt` companion comments — items are discovered by **iterating keys, never a hard-coded list** (the item set grows with template revisions; confirmed 72–73 items across real samples vs the handoff's "73"). Both base and `_cmt` keys are always emitted even when empty (confirmed on all three sample exports), so no orphan-comment handling needed. Installation info `mi_*` strings; `mi_certexp`/`mi_asidef`/`mi_classcc` are the attention flags. Executive summary `mi_ex_<pol\|reg\|eqp>_<bp\|nc\|ip>` — non-conformities (`_nc`) must be entered into Synergi, so they render prominently as the action prompt. **RETIRED at WCGRRT REV 155 (2026-09-09, Dan's decision — `DASHBOARD-ROLLING-HANDOFF.md` entry 1)**: no new Marine Integrity reports can be raised; the Marine tab is a **read-only archive** (labelled as such, nothing deleted), files that still arrive from older tool revisions are ingested exactly as before, their absence is never a fault, and the `marineData` identifiers stay reserved. **Routing**: `discipline === "Marine"` is excluded from the Well Control rig-visit list, KPIs, charts, and filter options — routed to the Marine Integrity tab instead — but stays in `reports[]` so the full-report viewer, report copies, and deep links still work | Full-report viewer's Marine section (`rvMarine`); Marine Integrity tab; `marineScores[]` (below) |
| `window.DASHBOARD_DATA.marineScores[]` (new, `Update-Dashboard.ps1` v2.31+) — top-level array sibling to `reports`/`cbmGrades`/`rigChecks`, **one record per report** (not per item): `{ rig, date, file, avgPol, avgReg, avgEqp, avgOverall, target, scoredCount, certexp, asidef, classcc, unit, imo, design, flagclass, client, field, ex: {pol_bp..eqp_ip}, items[] }`. `items[]` = `{ section, item, score, comment }` for every item with a score **or** a comment (fully-empty items dropped; `score` stays a string, `""` = commented-but-unscored). Averages passed through from the tool as-is including nulls (recomputing from raw scores is a valid cross-check — verified matching on real samples). Deduped `rig｜date`, newest file wins; newest-first; all dates kept per rig — that's what the Marine tab's score trending reads | Marine Integrity tab on the Reports Dashboard (fleet cards ranked worst-first + per-rig trend/action-list detail) |
| `tiles[].sbopData` / `tiles[].pdcData` / `tiles[].inspData` / `tiles[].vsrData` | report-type detection (and VSR rendering) |
| `photoDump[]` (`src` data URI or `""`, `caption`, optional `note: true`) | Photo dump & findings section in the full-report viewer |
| `meta.schedule` (P6 schedule name, free text), `meta.scheduleFile` (`data:` URL, PDF/image, optional), `meta.scheduleFileName` | Planning reports (`meta.discipline === "Planning"`): `schedule` is the row title on the reports dashboard and a "P6 Schedule" field + "View P6 Schedule" button in the full-report viewer. Only the short `schedule` name is copied into `reports-data.js`; `scheduleFile`'s base64 payload is read from the per-report copy the viewer already fetches, never the summary file |
| `meta.bopNo` (`"BOP1"` / `"BOP2"`) | Which BOP a Planning report's project targets — shown as a "BOP" field on the reports dashboard and in the BOP dashboard panel's title (`"Latest Planning Report — BOP1"`). **Note:** the per-rig Planning Report panel is still keyed by rig only, not rig+BOP — if a rig runs two simultaneous BWM projects (one per BOP) on different schedules, only the one with the newer `reportDate` shows; this is a known limitation, not yet addressed |
| Planning report filenames now also follow the BWM naming convention (`YYYYMMDD_<RIG>_<TYPE>_<BOPn>_Report.json`), not just the older `seadrill-report_<rig>_<date>.json` shape | No scanner impact — rig identity comes from `meta.asset`, never the filename (filename is only a last-resort fallback when `meta.asset` is blank) |
| `meta.reporttype === "Precharge"` (Seadrill BOP Precharge Calculator, Rev 31+'s "Export for dashboard" button — a standard version:3 `seadrill-report_<rig>_<date>_precharge.json`, **not** the tool's own Save/Load `.json` used for reloading inputs into the calculator, which is a different, non-dashboard file shape) | No new code needed — flows through the existing generic pipeline like any other report type. `tiles[0].title`/`notes` (HTML: well conditions + precharge summary table) render via the already-generic tile viewer; `meta.reporttype` auto-populates the type filter dropdown from real data, no hardcoded list to update. `meta.well`/`waterDepth`/`subseaTemp`/`surfaceTemp`/`requiredShear`/`mawhp`/`moc` are additional per-well context fields, not yet surfaced as their own dashboard fields but safe to add later. `tiles[0].prechargeData` (`conditions`, `verdict`, `summaryRows[]`) rides along unused, available for future bespoke rendering |
| `tiles[].topsetData` (`meta.reporttype: "TOPSET Investigation"`, WCGRRT REV 148+ — see `DASHBOARDTOPSETINVESTIGATIONHANDOFF.md`) — a Kelvin TOP-SET investigation **Terms of Reference**: scope, team, authority and evidence plan; **no findings and no causes exist in it by design** — never present it as an outcome. Identified by `topsetData` non-null (definitive), never by `meta.type` (users can retype it). Legs keyed `tech/org/ppl/sim/env/time` (two legs share the letter T — always key, never letter); `legs` always has all six keys, unused = `[]`; **empty leg + populated `leg_<key>_note` = "considered and ruled out" — a decision, not a gap**. `m*`/`ev*`(six)/`cf*` are real booleans, everything else strings/arrays; free text may contain newlines (plain textareas, no HTML). Status vocabulary on legs/deliverables: `Not started · In progress · Complete · Not applicable`. `torStatus` moves `Draft → Issued for approval → Approved → Superseded → Closed` **and can move backwards — never latch it** (same lesson as `projectComplete`). **Confidentiality (`cfClass`)**: anything above `Seadrill Internal` (`Confidential`, `Restricted — named recipients only`) is withheld at the DATA level — the scanner publishes a header-only record and excludes the whole file from `reports[]` and the deployed report copies (the open share has no access control). A scan-output line names each withheld ToR so Dan knows it exists | Full-report viewer's ToR section (`rvTopset`); Investigations tab; `topsetInvestigations[]` (below) |
| `window.DASHBOARD_DATA.topsetInvestigations[]` (new, `Update-Dashboard.ps1` v2.33+) — top-level array, **one record per INVESTIGATION** (not per file): keyed `rig｜torRef`, newest revision wins by tileDate then file mtime (fallback key `rig｜tileDate` + `missingRef: true` + scan warning when `torRef` is blank). Record: `{ rig, torRef, torRev, torStatus, severity, invLevel, raisedDate, date, eqName, cfClass, restricted, missingRef }` plus — **only when not restricted** — `{ file, teamLeader (team row whose role matches "Team Leader"), incDate, operation, synCaseTs, incidentNumber, legCounts {tech..time}, legRuledOut {tech..time}, overdue[] ({kind: evidence\|deliverable, leg, item, owner, due, status} — due < scan date AND status not Complete/Not applicable), quarantine {equipment, evidence, deadline, location, custodian} }`. Sorted newest-raised first. **Not a ranking input**: investigation counts must never be scored or compared between rigs (more investigations usually means better reporting culture) | Investigations tab on the Reports Dashboard (open list + fleet-wide overdue evidence/deliverables rollup + quarantine watch + closed list; restricted ToRs render as a locked header-only card) |

| Standalone **Compliance Checklist** (WCGRRT REV 151+ — see `DASHBOARDCOMPLIANCECHECKLISTHANDOFF.md`): `reportType === "compliance-checklist"` / `complianceOnly: true` / `meta.reporttype: "Compliance Checklist"`, filename prefix `seadrill-compliance-checklist_*` (deliberately NOT `seadrill-report_*`; filenames still aren't load-bearing). **`tiles` is always `[]` by design** — never treat that as a broken report. `checklist.statuses`/`notes` are **POSITIONAL with no item keys** — `complianceSummary` (the tool's precomputed per-priority P1–P8 tallies) is the only labeled data and is used as the authoritative numbers, never recomputed and never compared item-by-item across tool revisions. `""`/`blank` = **Not assessed — never folded into a pass**. **HISTORIC BUG (pre-REV-150)**: every earlier export saved `statuses`/`notes` as empty arrays (tool-side selector bug) — empty statuses are flagged `incomplete` and rendered as "no data", NEVER as 0-compliant. Appendix A/B registers, AAB totals, VRR and sign-off were always exported correctly (the cert-expiry watch works on historic files too). Dedup `rig｜date`, newest `exportedAt` wins. **REV 156**: top-level `actions[]` = Actions Raised During Visit (`{desc, system, resp, target, deadline (YYYY-MM-DD or empty), leftWithRig (bool)}`, always present, `[]` when none) — belongs to its parent report and is **replaced wholesale** with it, never merged. **REV 158**: each `actions[].photo` is a JPEG data URL or `""` (always present), and top-level `photos[]` (`{src, caption}`, ≤24) is the checklist's own photo dump, separate from the daily report's `photoDump` | Full-report viewer's compliance section (`rvCompliance`: summary → actions with thumbnails → notes → registers → AAB → photo dump → VRR → sign-off); Compliance tab; `complianceChecklists[]` (below) |
| `window.DASHBOARD_DATA.complianceChecklists[]` (new, `Update-Dashboard.ps1` v2.34+) — one record per rig｜date (all dates kept per rig for AAB trending), newest-first: `{ rig, date, dateEnd, file, bopNo, synCase, wce, exportedAt, incomplete, summary[] (P1–P8 passthrough), totals {compliant,na,action,blank,total}, actions[] ({id,label,action} where action>0), expiries[] ({register:A｜B, ele, desc, asset, cert, expiry} — placeholder rows dropped), vrr[], aab {reviewed,approved,returned,deferred}, signoff[], raisedActions[] (v2.40: `{idx, desc, system, resp, target, deadline, leftWithRig, hasPhoto}` — text only, the photo blob is fetched from the full report copy on demand via `idx`), photoCount }`. `incomplete: true` = empty statuses (historic bug): totals/actions suppressed at source so nothing downstream can misread them; expiries still populated. No fleet compliance league table anywhere | Compliance tab on the Reports Dashboard (latest-per-rig cards, **fleet-wide Actions Raised During Visits table — overdue derived from `deadline` < today, "NOT confirmed" when `leftWithRig` is false, 📷 button loads the photo from the full copy**, checklist priorities needing action, not-assessed badges, certification expiry watch ≤90 days) |
| `actionRows[].photo` on rig-visit reports (WCGRRT REV 158) — JPEG data URL or `""`, always present; the on-screen actions table is 9 columns (`# · Action · System · Responsible · Target · Deadline · Left w/ Rig · Photo · remove`). The scanner's `reports[].actionItems[]` carries `hasPhoto` only (v2.40) — **no blob ever enters `reports-data.js`** | Full-report viewer's Action items table shows the thumbnail (`actionsTable`); list views show counts only |
| `window.DASHBOARD_DATA.problems[]` (`Update-Dashboard.ps1` v2.40+) — every posted file the scan could not use: `{ file, kind, why, bytes, modified }`, `kind` ∈ `unreadable` (not valid JSON — the signature of a file cut short in transit) · `unrecognised` · `no-rig` · `error` · `large` (over the size ceiling, still ingested — **10 MB for every report type, 30 MB for CBM Inspection** since v2.41: a CBM condition record carries 48–100 already-compressed photographs by design, rolling handoff entry 6). Exists so a truncated post can never pass for a quietly empty report (`DASHBOARD-ROLLING-HANDOFF.md` entry 3) | Red banner under the source line, collapsed list of file names and reasons (amber when only oversized files) |
| **BOP Precharge REQUEST** (SSORT request form **Rev 3** — the frozen, live build; Rev 2 is the broken one — → calculator inbox; see `PRECHARGE-INBOX-1-FOR-DASHBOARD-SESSION.md` + `PRECHARGE-INBOX-DASHBOARD-REPLY.md`) — filename `seadrill-request_<rigKey>_<well>_<YYYYMMDD>_precharge.json`; identified by `meta.tool === "Seadrill BOP Precharge Calculator"` **and** `meta.source` starting `BOP Precharge Request form` (filename and `schema:1`+known `config` are secondary). The payload IS the calculator's own save schema (`meta`, `config`, `units`, `fields{k:{v}}`, `hops[]`) and is **copied verbatim, never reshaped** — it is fed straight into the calculator's `loadState()`. **A request is not a report**: excluded from `reports[]`, the fleet chart, the type filter and the deployed report copies; it exists only in the inbox index. Rig = `meta.asset` (Rev 2+), fallback `config`→name map (pre-Rev-2 files), then Unattributed. Request id = `<rigKey>_<well sanitised [^A-Za-z0-9._-]→->_BOP<bop>` — **no date** since v2.39 (`PRECHARGE-OWNERSHIP-AND-DATA-CONTRACT.md` §3.3: a re-issued precharge for the same well and stack is the same job); `rigKey` = `meta.rigKey`, fallback top-level `config`. Opaque; never parsed back. Dedup by id, newest `meta.saved` wins; **every payload that loses is kept verbatim as `requests/archive/<id>_<saved yyyyMMddTHHmmssZ>.json`** (F-23a), and the copy being overwritten is archived the same way. `archive/` is never pruned. The calculator's own "Post to Dashboard" output (`seadrill-report_*_precharge.json`, `meta.reporttype: "Precharge"`) is the **issued** sheet and stays a normal report | Scanner v2.38+ (ids/archive v2.39+): `<prechargeDeployPath>\requests\<id>.json` (verbatim copy) + `index.json` + `archive\`; consumed by the calculator session's `calculator.html` (Rev 74+, gated, Requests + Calculator tabs) (the standalone `inbox.html` was retired 2026-09-07) |
| **`<prechargeDeployPath>/requests/index.json`** (scanner v2.38+, the scanner is its ONLY writer — the calculator and inbox page only read) — `{ generated, requests[] }`, each `{ id, rig, rigKey, well, bop, raisedBy, email, workOrder, saved, received, shearReq, mawhp, waterDepth, hops (count), status, resubmitted, file }`, newest `saved` first. `status` ∈ `new` (id never seen) · `issued` (preserved from the previous index, OR set automatically when an issued precharge sheet — the version:3 envelope, matched on **`meta.rigKey` + `meta.well`** (contract §3.4; `bop` only disambiguates when *both* sides carry one, because `meta.bop` is empty on single-stack sheets by design) — has `meta.saved` ≥ the request's — the return leg) · `opened` (recorded by the inbox page in the browser's localStorage only; never in this file). A re-post of a known id **never resets `issued`**; it sets `resubmitted: true`. Request copies whose source file is gone are removed. Nothing is written until `prechargeDeployPath` is set in `config.json` — the payloads carry MASP/water depth/shear and must sit behind IIS Windows Authentication, not in the open share | the Requests tab of `calculator.html` (calculator session; new-at-top, amber > 48 h, row click loads the request) |

Adding NEW keys anywhere remains safe and is still the right way to extend.


## Safe changes (no dashboard impact)

- **Adding new fields anywhere** — the scanner and dashboard ignore unknown
  fields. This is the right way to extend: add, don't rename or repurpose.
- Any UI/styling/workflow change that doesn't alter `buildReportPayload()`.
- New tile types: give them a sensible `title` and they'll render; add their
  structured data as a **new** key (like `vsrData`) rather than changing
  existing keys.
- Adding entries to dropdowns (rigs, visit classifications, disciplines).
- Changing the Word/Excel exports — the dashboard only reads the `.json`.

## If a breaking change is truly needed

Bump `version` (e.g. to 4), keep writing the old fields alongside the new
ones if at all possible, and tell Dan Plant the dashboard needs a matching
update — the scanner and dashboard live in the
`dplant494-svg/Dans-projects` repo and must be updated in the same breath.

## How to verify nothing broke (2 minutes)

1. Export a test report from the edited tool.
2. Drop it in the scanned folder and run
   `scripts\Update-Dashboard.ps1` — it must report the file in the count,
   with **no "Skipping …" warning** for it.
3. Open the dashboard: the visit appears with rig/dates/WCE/critical/action
   values filled in, and **View full report** renders the entries, notes,
   and photos.
