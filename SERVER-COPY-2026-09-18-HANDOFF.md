# Handoff — Dan is copying `C:\TSC-Dashboard` to the sacred server today. Is your latest on his C drive?

**From:** Dan Plant, via the dashboard / scanner session (scanner v2.57)
**To:** the Precharge Pro session (now running in Claude Code, for continuity), the
reporting-tools session (WCGRRT / SSORT), and any other session that has put a file on
`\\sdrlazneuiis01d.corp.local\sacred` or `\\sdrlazneuiis01d.corp.local\SSORT`
**Date:** 18 September 2026
**Reply wanted:** yes, a short handoff back (format at the end). If you have nothing
newer than what is on the C drive, say that in one line.

---

## 1. What is happening

Today Dan copies the **whole** of `C:\TSC-Dashboard` (his PC's install: scripts, config,
pages, calculator files, state) to the server share, into a staging folder
`\\sdrlazneuiis01d.corp.local\sacred\_INSTALL-TSC-Dashboard\`, exactly as
`SACRED-SERVER-MOVE-STEPS.md` step 2 describes. IT then moves it to `D:\TSC-Dashboard`
on the server and runs the scanner's scheduled task from there instead of from Dan's PC.

The copy goes into a **staging subfolder**. It does not land on top of the published
folders (`sacred\dashboard\`, `sacred\precharge\`, the BOP and SSCE pages). Nothing you
have published on the server is overwritten by the copy itself.

The risk is the other way round: once IT runs from the server copy, **the server copy is
the master**. `Deploy-Dashboard.ps1` on the server will publish whatever is in
`D:\TSC-Dashboard\precharge\` and `D:\TSC-Dashboard\dashboard\`. If the C drive holds an
older build than the one you last put on the server, the next deploy from the server
would put the older build back. That is what this note prevents.

## 2. What Dan needs from you, before he copies

Check the files you own against the C-drive copy and tell him which is newer.

### Precharge Pro session

The C drive should hold your latest builds in `C:\TSC-Dashboard\precharge\`:

| File | Owner | Where the browser sees it |
|---|---|---|
| `calculator.html` | you | `sacred\precharge\calculator.html` |
| `set-password.html` | you | `sacred\precharge\set-password.html` |
| `gate-config.js` | Dan (generated by set-password) | `sacred\precharge\gate-config.js` |
| `gate-fragment.html` | dashboard session | `sacred\precharge\gate-fragment.html` |

Please confirm the **revision number** of the calculator you last delivered (Rev 82 by
the dashboard session's records) and that the copy Dan holds at
`C:\TSC-Dashboard\precharge\calculator.html` is that build. If you delivered anything
newer straight to the server, or to Dan by a route that did not go through
`C:\TSC-Dashboard\precharge\`, name the file and where it is now.

Two things you do **not** need to worry about:

- `sacred\precharge\requests\` (the inbox: `index.json` and the request payloads) is
  written only by the scanner on the server, every ten minutes. It is not in the C-drive
  copy and the copy does not touch it.
- The precharge calculator arithmetic and the posting flow are not part of this move.
  Nothing changes in the data contract (`PRECHARGE-OWNERSHIP-AND-DATA-CONTRACT.md`).

### Reporting-tools session (WCGRRT / SSORT)

Your tools post to SharePoint through the Power Automate flow; the scanner reads the
synced folders. Nothing of yours lives in `C:\TSC-Dashboard`, so the copy does not carry
your files. Two questions only:

1. Have you placed anything directly on `\\sdrlazneuiis01d.corp.local\SSORT` (for
   example a newer COC dashboard revision than `Seadrill_WCE_COC_Dashboard_REV6 1.html`,
   which is the file the scanner's `cocDashboardPath` points at)? If so, name the file,
   so IT's config points at the right one.
2. Anything the server-side scanner should know about a build you are about to release
   (REV 161, the Post to OEM button): nothing changes for you, but say if a filename or
   folder is about to move.

### Any other session

If you have put a page, a data file or a script anywhere under `sacred\` or `SSORT\`,
list it with its date. If not, one line saying so.

## 3. What the dashboard session confirms about the C drive (so you can trust it)

As of 18 September the C drive holds, verified by Dan's own scan output this morning:

- `scripts\Update-Dashboard.ps1` **v2.57**, `Deploy-Dashboard.ps1`,
  `Register-DashboardTask.ps1` (60-minute limit), `Archive-ProblemFiles.ps1`.
- `dashboard\dashboard.html` at the 16 September build (Errors button, Ask SACRED AI,
  soak and PDC viewers, print rules). Published to the server the same day, so the
  server and the C drive match.
- `precharge\gate-fragment.html` and `precharge\inbox-fragment.html` as delivered.
- `archive\2026-09-17\unattributed\` with the eight pre-rig-guard posts Dan moved out
  yesterday (one of them 74.8 MB). These are not needed on the server; IT can leave
  them out or delete them there.
- State files `scan-state.json`, `scan-lock.json`, `notified-state.json`,
  `break-ins-*.json`, `ssce-notified-state.json`: harmless to copy; the server scanner
  rebuilds them on its first run.

Generated files on the **server** that the copy must never replace and that the scanner
regenerates anyway: `sacred\dashboard\reports\` (the 283 report copies),
`sacred\dashboard\reports-data.js`, `sacred\bop-planning-data.js`,
`sacred\ssce-requests-data.js`, `sacred\precharge\requests\`, `break-ins-pending.json`.
The staging-folder copy does not touch any of them.

## 4. What IT still has to settle (not for you, listed so nobody assumes it is done)

- `config.json` on the server needs the three `reportFolders` pointed at server-local
  mirrors of the SharePoint libraries (`config.server.json` has the placeholders and
  the migration handoff §2 has the options). Dan's paths are OneDrive folders under his
  profile and do not exist on the server.
- `digestPath` (the Copilot digests library, also OneDrive-synced on Dan's PC) needs
  the same treatment, and `dashboardUrl` / `copilotUrl` carry over from Dan's
  `config.json`. The dashboard session will refresh `config.server.json` when IT
  confirms the mirror paths.
- Dan's PC keeps its scheduled task running until the server run is proven. Two
  scanners in parallel is safe: they write the same outputs.

## 5. The handoff back

One short file, `SERVER-COPY-2026-09-18-REPLY-<session>.md`, with:

```
Session: <Precharge Pro | reporting-tools | other>
Files I own on the server or on Dan's C drive:
  <file>  C drive: <rev/date or "not there">  Server: <rev/date or "never put there">  Newer copy: <C | server | same>
Anything Dan must copy into C:\TSC-Dashboard\ BEFORE he copies to the server: <list, or "nothing">
Anything about to change (filename, folder, revision): <list, or "nothing">
```

If everything you own is already on the C drive at the latest revision, the whole reply
is one line: "Nothing newer than C:\TSC-Dashboard, go ahead."
