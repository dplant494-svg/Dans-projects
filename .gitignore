# gate-config.js and the requests folder are per-installation, never committed
precharge/gate-config.js
precharge/requests/
# built by the calculator session (PRECHARGE-OWNERSHIP-AND-DATA-CONTRACT): Dan drops them in, Deploy publishes them as-is
precharge/calculator.html
precharge/set-password.html
# written by Update-Dashboard.ps1 v2.45 next to config.json; per-installation, never committed
scan-state.json
# written by Update-Dashboard.ps1 v2.57 while a full scan runs; removed at the end
scan-lock.json
# written by Update-Dashboard.ps1 v2.58: photo-stripped report copies for faster scans; per-installation
scan-cache/
# AAB loop (scanner v2.65): per-installation, never committed
aab/gate-config.js
aab/aab-data.js
aab-overdue-pending.json
aab-overdue-state.json
