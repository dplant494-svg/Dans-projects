# The week of 14 September 2026 — what gets built, in what order, by whom

**Owner:** Dan · **Written:** 13 September 2026 by the dashboard / scanner session
**Rule:** one thing at a time, top to bottom. Each item says who does it, what "done"
looks like, and what it waits on. Tick them off here.

| # | Item | Who | Waits on | Done when |
|---|---|---|---|---|
| 1 | **Copilot on the reports, live** — **DONE 13 Sep.** Agent on `Digests/Reports`, published to Teams, button on the dashboard, milestone M1a in the timeline reply | Dan + scanner v2.44 | — | Done |
| 2 | **Precharge ISSUED email carries the PDF, not the JSON** — **DONE 14 Sep.** Rev 82 deployed, flow attachment rewired (`base64ToBinary` on `sheetPdf`), real issued post received externally with a two-page PDF that opens and matches the download | Dan (deploy + flow) | — | Done |
| 3 | **Large reports: photos shrunk at source** | reporting-tools session (WCGRRT + SSORT) | handoff below | A 100-photo CBM report posts under 10 MB, photos still legible |
| 4 | **Scanner remembers unchanged files** (v2.45) — **built, installed and proven in production 14 Sep** | scanner session | nothing | Scan of 270 files completes in seconds when nothing changed |
| 5 | **Daily Checks out of the Report List, into Rig Monitoring** — **built 13 Sep, installed 14 Sep** | scanner session (dashboard change) | nothing | Report List shows visits, CBM, investigations, compliance, precharge; Daily Checks / FLM live only on Rig Monitoring with trends and a per-rig submissions list |
| 6 | **Rig visit report email with a dashboard link** — guide written 13 Sep: `RIG-VISIT-NOTIFICATION-FLOW-GUIDE.md` | Dan builds the flow from the guide | #1 | A posted rig visit report emails its recipients a link to that report on the dashboard, no PDF attached |
| 7 | **Can Dan create in Azure / Dataverse / Power BI?** — **DONE 13 Sep.** Azure: no subscription. Dataverse: exists, no create rights. **Fabric: yes** — a SQL database was created in Dan's workspace on a 60-day Fabric trial. | Dan | — | The database can start **now**; the January IT ask is a paid Fabric capacity, not an Azure instance |
| 8 | **SPARC handoff from Lee** — **received 14 Sep**, filed as `SPARC-HANDOFF-SACRED.md`, answered in `SPARC-HANDOFF-REPLY.md` (Maximo: no live link, but a scheduled daily export at 06:00 Houston already feeds SPARC and the COC tracker; field mapping banked; Manpreet produces the export, landing folder unknown, Dan waiting on an update; master workbook still to ask Lee for). Photo value settled: 0.82 in both tools (Dan, 14 Sep) | Lee → Dan → reporting-tools session | — | Done |
| 20 | **Every automated notification carries a link to the report on the dashboard** (Dan, 16 Sep: "forces everyone to go to the dashboard, gets us more clicks, looks better"). Precharge issued email: already has it. Rig visit flow: in the guide (`Link` card, `?report=<file>`). CBM to OEM: added to the guide as an internal-readers line, since NOV cannot reach the dashboard. Rule added to the loop pattern checklist: no loop ships without the link | Dan (as each flow is built) | — | Every notification email opens the report on the dashboard in one click |
| 19 | **Three surface tests never saved or posted** (rolling handoff entry 13, 16 Sep): Acoustic Function Testing, EHBS Testing and Surface Drawdown Test live in iframes inside WCGRRT and `collectSoak` cannot see into them, so their contents print to the PDF and go nowhere else. **Authorised by Dan, 16 Sep evening:** harvest the iframe fields into `soak`, fail loudly on the fallback path, ship `soakLabels` in the same revision. Brad's EDS records came from a spreadsheet merged into the PDF, so no fourth path leaks. Nothing for the dashboard until the payload carries the data. Brad's 8–15 Sep test records exist only in his PDFs; keep them | Dan (decision) · reporting-tools (fix) | — | A posted report with an EHBS, acoustic or drawdown test carries the test in `soak` and the viewer shows it |
| 18 | **Pre-deployment checklist, SSORT REV 146** (rolling handoff entry 10). Scanner v2.50: `pdcData` gets the 40 MB ceiling; viewer renders the checklist per BOP with the mandatory photo sections. **Built 16 Sep**; a real REV 146 post wanted to confirm on real data | scanner session (done) · reporting-tools (sample) | — | A posted PDC shows its packers attestation and 2×cavities photographs in the viewer, not under Errors |
| 17 | **Daily reports were overwriting each other** (WCGRRT ≤ REV 160 named the file after the visit start; entry 11, Brad, 16 Sep). Tool fix REV 161 is theirs. Ours, built 16 Sep: report date from the first entry (`reportDate`), shown with visit span and posted time; `soak` (EDS + function tests) rendered; print keep-together rules; print CSS sent. **Recovery, 16 Sep afternoon:** Brad's own saved copies arrived; only the 8 Sep report was lost, and Dan drops that file into PostedReports as `seadrill-report_West-Capella_2026-09-08_daily-report.json`. **Correction:** the four real files carry `soak = {}` on every entry, so the EDS and function tests are NOT in the posted JSON at all; renderer built and idle, tool-side trace requested (reply §12.1). Scanner v2.51: report date = newest entry date (the 15 Sep file holds the 14th and 15th) | Dan (drop the 8 Sep file) · reporting-tools (REV 161 + soak trace) · scanner session (done) | — | Four Capella dailies on the dashboard under 8, 10, 12 and 15 Sep; the tests appear once the tool posts them |
| 16 | **Colleagues cannot install the WCE Reports Assistant in Teams** (Dan, 15 Sep: "Your organization has prevented this agent from being installed"). Teams app policy, not the agent. Dan: Copilot Studio → Channels → Teams → Availability → **Show to everyone in my org** → Submit for admin approval. IT: Teams admin center → Manage apps → publish it and allow it in the app permission policy. **Submitted for admin approval by Dan, 16 Sep.** Waiting on IT to publish it | Dan (done) · IT (approve) | IT | A colleague clicks Ask SACRED AI and it opens in Teams |
| 15 | **Test mode for the notification flows** (Dan, 16 Sep: no test mail to rigs or NOV). A `Settings` table in the notification workbook, `TestMode` TRUE/FALSE + `TestEmail`; every flow sends everything to TestEmail with `[TEST]` in the subject while TRUE, live when FALSE; no address ever swapped. Guide: `NOTIFICATION-TEST-MODE-GUIDE.md`. Apply to the precharge flow and build it into the rig visit and CBM flows as they are made | Dan | — | TestMode TRUE: a test precharge post mails only Dan; FALSE: the rig |
| 14 | **CBM reports to NOV automatically** (Dan / Dave Cargill, 15 Sep). Loop three of the pattern: SSORT gets a **Post to OEM** button that posts the report PDF as a small `oem-copy` file through the existing trigger (`CBM-OEM-HANDOFF.md`, to the reporting-tools session); a `CBM to OEM` flow emails it to the NOV sheet in the notification workbook, Dave Cargill first, office in CC (`CBM-OEM-NOTIFICATION-FLOW-GUIDE.md`, Dan builds); scanner **v2.49** + dashboard record the copy and show a green *Sent to NOV* chip on the CBM row. **Scanner/dashboard side built and verified 15 Sep**; tool button and flow to follow | reporting-tools session (button) · Dan (sheet + flow) · scanner session (done) | button | Press Post to OEM on a CBM in SSORT: NOV list gets the PDF, office copied, dashboard row says Sent to NOV |
| 13 | **The Maximo item export, read** — Dan sent SPARC's five `SDITEM_SFI` files 15 Sep. Profiled (`MAXIMO-ITEM-EXPORT-PROFILE.md`): Maximo saved-query reports as Excel 2003 XML, 65,720 rows, **22,170 active items**, SFI 331/332/334/335/336 (no 333), generated 12 Jun 05:01–06:10 (the scheduled chain). `scripts/Convert-MaximoItemExport.ps1` (PS 5.1) reads them by header name into one CSV + summary, proven row-for-row. The "which files" half of the Maximo question is answered; "where they land" is still Manpreet's | scanner session | landing folder (Manpreet) | Reader runs on the daily export from its landing folder; the two reference tables load from its CSV |
| 12 | **Assistant Rig Managers on the precharge REQUEST and ISSUED emails** (Dan, 15 Sep). One new column `ARMEmail` on the Rigs sheet (added in the same sitting as the rig-visit columns), then two flow edits: ISSUED puts the ARM in **To** beside the Subsea Supervisor and TSL; REQUEST adds the rig's ARM in **CC** (the office stays To). Needs the RigRow lookup available on the request branch too. Guide to follow once the rig-visit flow is built, same pattern | Dan (workbook + flow), guide from scanner session | item 6 first | A test request and a test issued post both reach the address in the ARMEmail cell |
| 11 | **Rolling handoff entry 9 — CBM ceiling 40 MB** (SSORT REV 146 at photo quality 0.82). Scanner **v2.48**, built 14 Sep late; Dan installs tomorrow with the usual two lines | scanner session | — | Scan says v2.48; a CBM report between 30 and 40 MB no longer appears under Errors |
| 10 | **Rolling handoff entry 7 — potable-water flush observation items** (SSORT REV 145). **Built and verified 14 Sep**: inverted attention rule for the two items on Rig Monitoring (tile badge, matrix cell, per-rig count line, submissions column), viewer row, digest note (scanner v2.47), contract rule for the database view. **Installed 14 Sep** with scanner v2.47 | scanner session | — | A round that ticks the flush without a comment shows amber on Rig Monitoring, and the per-rig count answers the VP's question |
| 9 | **Dashboard sweep, 14 Sep** — Marine tab gone, Errors button instead of the red banner, gold **Ask SACRED AI** button, and `Archive-ProblemFiles.ps1` to move the unreadable posts out of the folders. **Built and installed 14 Sep** | scanner session | — | Report List has five tabs, Errors pill opens the list, unreadable posts archived and off the Errors list |

## Installed on Dan's PC, 14 September, 20:17

Scanner **v2.47** and the sweep `dashboard.html` are live and deployed. From the run:

| Seen | Meaning |
|---|---|
| 279 files, 258 reports, 273 digests | full estate, no file skipped |
| **0 unreadable files** (the "9 could not be read" from the morning are gone) | they were OneDrive downloads still in flight when the scan read them, not broken posts. The Errors button now shows only oversized reports. Nothing to archive. |
| 4 oversized: vendor audit 74.8 MB (25 Aug), vendor surveillance 29.9 MB (3 Sep), **West Capella daily 10 Sep 10.0 MB and 12 Sep 19.3 MB** | the first two are pre-fix artefacts. **The two Capella dailies post-date WCGRRT REV 157's photo shrinking** and should not be that size; raised with the reporting-tools session (rolling handoff reply). Likely an old cached copy of the tool on that PC. |
| 7 files under Unattributed (two `TMP` reports, the vendor files, a 29 Aug daily), all pre-REV-153/159 | historic; the source fix stops new ones. They stay unless Dan wants them archived too (the archive script does not touch them today; one word and it will). |
| Marine Integrity: 3 records across 2 rigs | answers the rolling handoff's entry 1 question; the tab is gone, the records are not |
| Second scan: `Nothing changed since the last full scan` | v2.45 short cut working in production |
| First Deploy: `Deploy folder not reachable`, second worked | a transient share drop; the scanner's own deploy is guarded the same way (no state recorded on failure, full retry next run) |

## 1. Copilot — today

Follow `COPILOT-REPORTS-ASSISTANT-GUIDE.md`. State at 13 Sep: library created and
synced, config written, **v2.43 ran and must be replaced by v2.44** (delete the
Digests folder contents, install v2.44, rerun), then Parts D and E. Part F is a
two-minute tightening of the precharge flow.

## 2. Precharge email — the PDF, and only the PDF — done 14 September

Verified: a real issued post from Rev 82 reached an external inbox with the two-page
PDF attached, opening and matching the download. The fix on the flow side was one
wrap, `base64ToBinary(...)` around `sheetPdf` in the attachment Content, because the
Outlook connector wants bytes and a bare base64 string gets encoded a second time.
The Precharge Pro session's §3 "unverified" point is closed by that one post.

Dan's standard: the attachment the rig receives must be **the generated PDF, exactly**.
The only way to guarantee "exactly" is to send the PDF itself, so the calculator puts
the PDF bytes in the post and the flow attaches them. Handoff:
`PRECHARGE-HANDOFF-SHEETPDF.md`. Flow change once it lands: attachment 1 removed,
attachment 2 becomes the PDF (two clicks, instructions in the handoff). No scanner
change.

## 3. Large reports — at source

Handoff: `LARGE-REPORTS-PHOTO-HANDOFF.md` to the reporting-tools session, with the
numbers. Nothing on the scanner side changes; the 10 MB / 30 MB ceilings stay as the
warning that tells us the fix is working.

## 4. Scanner cache — v2.45, built 14 September

"Recheck and post only if modified", exactly, and built the simpler of the two ways.
The plan above said a per-file cache. Reading the loop again: one file feeds fifteen
different aggregates (reports, precharge inbox, planning, day log, rig checks, CBM
grades, TOPSET, compliance, digests, report copies...), so a per-file cache means
recording every one of those per file and replaying them, which is a rewrite of the
900-line loop with no real data here to prove it against. Principle 4 of the
Precharge Pro session's document applies: a cache that quietly measures less than the
full scan is worse than no cache.

So v2.45 does the honest version. Every output of the scan is a pure function of the
input files, `config.json`, the script version and today's date. The scanner hashes
exactly those (every scanned file's name, size and last-modified time, the config
text, the version, the date) and records the hash in `scan-state.json` next to
`config.json`, **only after a run that found reports and completed every deploy
step**. The next run computes the same hash first: if it matches, nothing can have
changed, so it refreshes the "Data updated" stamp in the three data files and their
deployed copies and stops. One changed byte in any file, a new file, a removed file,
an edited config, a new day, a failed deploy last time, a missing deployed file, or
`-Force`: full scan, identical to v2.44.

Measured here on the 16-file test set: full scan 9.1 s, unchanged run 0.8 s. On
Dan's 270 files the unchanged run is the same 1 s; it never opens a report. The
10-minute task therefore does real work only in the ten minutes after a post, and at
most once a day otherwise (the date is in the hash because TOPSET overdue flags
compare due dates with today).

Also in v2.45: digests say **"Critical items (0): None recorded"** and **"Actions
raised during the visit (0): None recorded... anything that reads like a task in the
text below was not raised as an action"** when a report raised nothing. Copilot was
improvising actions from narrative; now the digest tells it there were none. First
v2.45 run rewrites every digest once (266 small files sync, Copilot re-indexes in
minutes).

**Install (Dan):** save `Update-Dashboard.ps1` over
`C:\TSC-Dashboard\scripts\Update-Dashboard.ps1`, then in PowerShell:

```
Unblock-File C:\TSC-Dashboard\scripts\*.ps1
C:\TSC-Dashboard\scripts\Update-Dashboard.ps1
C:\TSC-Dashboard\scripts\Update-Dashboard.ps1
```

First run: the full scan as today, ending with the usual Deployed lines. Second run:
one green line, `Nothing changed since the last full scan at ...`, in about a second.
Post anything, run again: full scan. No config change, no flow change, nothing for
the other sessions. For the Precharge Pro session, one line: the inbox `index.json`
`generated` stamp now moves only when a scan actually ran, which is exactly when its
content can have changed.

## 5. Daily Checks off the Report List

Same routing the Marine reports already have: Daily Checks and FLM stay in the data,
the viewer and the digests, but leave the Report List, the KPIs and the charts. Rig
Monitoring becomes their home, with the per-rig readings matrix and trends it already
has, plus the list of submissions per rig so a specific day's checks can be opened
from there. Dashboard change only.

## 6. Rig visit report email — loop two of the pattern

Same shape as the precharge flow, `NOTIFICATION-LOOP-PATTERN.md` §6 checklist:
trigger on the file, kind from `meta`, recipients from the existing
`Seadrill_WCE_Notification_Distribution_Matrix.xlsx`, one email with the rig, dates,
who visited, the count of actions raised, and the dashboard link. No attachment. A
click-by-click guide follows once #1 is live, because the flow is built the same way
and Dan has just done one.

## 7. Azure / Dataverse / Power BI — checked 13 September, answered

| Where | Result |
|---|---|
| Azure portal | **No subscriptions** visible to Dan's account. Cannot create anything. |
| Dataverse (Seadrill Apps PROD) | Database exists; "Start with a blank table" greyed out, banner says commands unavailable for Dan's privileges. **No create rights.** |
| Power BI / **Fabric** | Licensed workspace. Fabric items available, including **SQL database**, **Warehouse**, **Data agent**, **Mirrored SharePoint Online List**. Creating a SQL database **auto-started a 60-day Fabric trial** and created `wce-test-delete-me` with a T-SQL query window and connection strings. |

**What this changes.** The database can be built and proven now, on the trial, by us,
with no IT resource: the engine is Azure SQL under Fabric, so the recommendation
already agreed with the reporting-tools session stands. The January ask to IT becomes
**"attach a paid Fabric capacity to the WCE workspace before the trial ends"**, which is
a licence line, not an infrastructure project. The trial deadline is the date to prove
the schema, the loader and parity by.

**One open technical question, ours:** how the scanner authenticates to the Fabric
database from a scheduled task with no installs. Fabric SQL uses Entra sign-in, not a
SQL password. The clean route is an app registration (service principal) whose token
the scanner fetches with a plain web call and hands to `SqlConnection`; the app
registration is a small Entra admin ask and belongs on the same ticket as the service
identity IT already want for notifications. To be answered in the timeline reply.

**Not for production on the trial.** Anything built there must move to a paid
capacity or be re-created; the pack says so plainly.

Original checks, for the record:

- **Azure:** `https://portal.azure.com`, sign in with the Seadrill account. Click
  **Create a resource**, search **SQL Database**, click **Create**. If the
  **Subscription** dropdown offers a subscription, Dan can create. If it says *no
  subscriptions* or the page is blocked, the database instance is an ISIT ask, as the
  plan already says.
- **Dataverse:** `https://make.powerapps.com`, environment **Seadrill Apps PROD** (top
  right), left menu **Tables**, **+ New table**. If a table can be created, Dataverse
  is available now. It is native knowledge for Copilot Studio and native to Power
  Automate, with no server and no ISIT resource.
- **Power BI:** `https://app.powerbi.com`. If it opens to a workspace with **+ New
  report**, there is a licence. If it offers a trial, there is not.

What the answers decide: with Azure SQL available now, the database starts now on
the plan already agreed with the reporting-tools session. With only Dataverse, the
database can still start now, and the engine recommendation in the timeline reply
needs revisiting with them, because Dataverse changes how the scanner writes (through
a flow or the Web API, not a SQL connection). With neither, January, as planned.

**On "we would not need all these HTMLs":** the dashboards stay, by Dan's decision
(same pages, ISIT maintain them). What a database replaces is the *digests*: Copilot
Studio can read Dataverse tables directly, and Azure SQL through a connector, so once
the data is in a database the agent is pointed at that and the Digests library is
retired. The digests are the bridge that gets Copilot live this week rather than in
February.

## 9. Dashboard sweep — 14 September

Dan's list from the screenshot, all in one install:

- **Marine Integrity off the dashboard**, not archived. Tab and view removed. The
  scanner still ingests any Marine file that arrives (nothing is deleted) and the
  full-report viewer still shows the marine section inside an old report.
- **Errors button instead of the red banner.** A pill at the right of the tab strip,
  `⚠ Errors (n)`, red when files are missing from the dashboard, amber when only
  oversized reports remain, hidden when there is nothing. Click it for the list in a
  panel, split into "Not on this dashboard" and "Oversized, still shown".
- **Ask SACRED AI.** The Copilot button is now gold, bigger, with a ✦, and says
  "Ask SACRED AI". Same link, same agent.
- **The unreadable posts.** They are the nine "could not be read" files: posts cut
  short in transit, carrying nothing usable. Scanner v2.46 writes the Errors list
  with full paths to `scan-problems.json`, and the new `Archive-ProblemFiles.ps1`
  moves those files out of the report folders into `C:\TSC-Dashboard\archive\<date>\<kind>\`
  (nothing deleted, every move logged in `ARCHIVED.log`). It shows the list and waits
  for a Y first. The five oversized reports are **real reports and stay**; they are
  on the dashboard, and the fix for them is item 3 (photos shrunk at source).
  The script has no option to move them (Dan: "I don't want the ones with too many
  photos out").
- The red ⚠ next to Daily Checks rows in the screenshot goes away by itself once the
  item-5 `dashboard.html` is installed: those rows leave the Report List for Rig
  Monitoring.

**Install (Dan):**

1. Save `dashboard.html` over `C:\TSC-Dashboard\dashboard\dashboard.html`.
2. Save `Update-Dashboard.ps1` over `C:\TSC-Dashboard\scripts\Update-Dashboard.ps1`.
3. Save `Archive-ProblemFiles.ps1` into `C:\TSC-Dashboard\scripts\`.
4. PowerShell, one line at a time (R if asked):

```
Unblock-File C:\TSC-Dashboard\scripts\*.ps1
C:\TSC-Dashboard\scripts\Update-Dashboard.ps1
C:\TSC-Dashboard\scripts\Archive-ProblemFiles.ps1
```

   The archive script lists the nine files and asks `Move these 9 file(s)? Type Y`.
   Type `Y`, Enter.

```
C:\TSC-Dashboard\scripts\Update-Dashboard.ps1
C:\TSC-Dashboard\scripts\Deploy-Dashboard.ps1
```

5. Open the dashboard, Ctrl+F5. Five tabs, an amber `Errors (5)` pill (the oversized
   five), the gold Ask SACRED AI button top right.

## 8. SPARC — received 14 September

Lee's handoff is filed unchanged as `SPARC-HANDOFF-SACRED.md`. The reply,
`SPARC-HANDOFF-REPLY.md`, goes to the reporting-tools session with the Maximo answer:
**no live link, but a scheduled daily Maximo export (06:00 Houston) already feeds
SPARC and the COC tracker.** The route exists; our tools do not read it yet. Field
mapping banked, §9's two weeks stand. Lee's answer needed: where the export lands and
which files it holds. Next steps in the reply: load
`SPARC_Master_Data.xlsx` into the Fabric database as reference tables, serve SPARC from
IIS on the same server, then a dashboard link. Three questions back to Lee.
